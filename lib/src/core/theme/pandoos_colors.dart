import 'package:flutter/material.dart';

// ─── Brand Colors ─────────────────────────────────────────────────────────────
class PandoosColors {
  // Deep space background
  static const Color background   = Color(0xFF080811);
  static const Color surface      = Color(0xFF12111E);
  static const Color surfaceHigh  = Color(0xFF1C1A2E);

  // Purple accent (brand primary)
  static const Color primary      = Color(0xFF9C6ADE);
  static const Color primaryGlow  = Color(0x559C6ADE);
  static const Color primaryDark  = Color(0xFF6B3FA8);

  // Cyan accent
  static const Color accent       = Color(0xFF56E3D8);
  static const Color accentGlow   = Color(0x4456E3D8);

  // Pink for moods / love
  static const Color pink         = Color(0xFFE356A7);
  static const Color pinkGlow     = Color(0x44E356A7);

  // Text
  static const Color textPrimary   = Color(0xFFF0EEFF);
  static const Color textSecondary = Color(0xFF9896B0);
  static const Color textMuted     = Color(0xFF4E4C68);

  // Semantic
  static const Color success = Color(0xFF56DEB4);
  static const Color error   = Color(0xFFE35656);
  static const Color warning = Color(0xFFE3B756);

  // Glass / blur overlays
  static const Color glassLight  = Color(0x1AFFFFFF);
  static const Color glassBorder = Color(0x26FFFFFF);
  static const Color glassShade  = Color(0x0AFFFFFF);
}
